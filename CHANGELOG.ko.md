# 변경 이력

## 0.1.0-alpha.4

설정 카드에 서랍(드로어) 형태를 복원: 이전에는 맨 행만 있어 「플러그인」 설정 섹션의 다른 플러그인과 일치하지 않았다.

### 수정

- **설정 카드에 드로어 외피가 없음(중요)**: DSH 의 「플러그인」 설정 섹션
  (`dsh-client-ui-settings-plugins`)이 하는 일은 단 하나——settings 네임스페이스별로
  `settings.plugin.item` 을 배포하는 것:

      renderSlot('settings.plugin.item', {}, { entryKey: ns })

  외피는 **전혀 제공되지 않는다**. 카드의 제목·설명·펼침/접힘은 모두 플러그인 자신의 소유다.
  이 플러그인은 맨 행만 렌더링했기 때문에 두 가지가 어긋났다:
  1. 섹션 안 다른 플러그인의 「접이식 드로어」와 형태가 맞지 않음;
  2. `card.title`(세션 스튜어드)과 `card.desc`(이력 파일 · 상태 점검)가
     **정의만 되고 한 번도 렌더링되지 않는** 죽은 문구가 됨——이것이 「빠진 드로어 항목」.

  이제 헤더(제목 + 설명 + chevron, 블록 전체 클릭 가능)를 복원하고
  본문은 기본적으로 접힌다(같은 섹션의 다른 카드와 동일). `aria-expanded` 는 펼침 상태를 반영한다.
- **설정 서비스를 쓸 수 없을 때의 동작**: 이전에는 카드 전체가 「설정 서비스를 사용할 수 없음」 한 줄로
  퇴화하며 **제목까지 사라졌다**. 이제는 드로어 헤더를 정상적으로 렌더링하고
  사유는 본문에만 표시한다——이 플러그인이 섹션에서 완전히 사라지지 않는다.

### 변경

- 펼침 전환과 chevron 회전은 `prefers-reduced-motion` 을 따른다.

## 0.1.0-alpha.3

상태 점검 결과 캐시: 패널을 닫았다 열면 지연 없이 내용이 나오고, 처치 후에는 그 행만 갱신된다.

### 추가

- **상태 점검 결과 캐시(프로세스 내)**: 한 번의 완전 스캔(분할로 말뭉치를 끝까지 도는 것) 결과를
  호스트 프로세스에 보관하고, 패널을 다시 열면 캐시를 그대로 렌더링한다(전량 재스캔 없음).
  캐시는 **말뭉치를 한 바퀴 다 돌았을 때만** 성립한다——중간에 패널을 닫아도
  반쪽 결과가 완전한 결론인 척하지 않는다.
- **`resume` 은 순수 읽기**: `session-health-scan` 에 `resume: true` 를 추가. **캐시만 읽고 스캔하지 않는다**.
  패널은 마운트 시 이것으로 한 번만 무비용 탐색하고, 적중하면 내용을 내고 빗나가면 아무것도 하지 않는다
  (마운트 즉시 중작업은 이전 버전에서 지적된 지점). 동시에 현재 말뭉치 총수를 돌려준다.
- **생성 시각과 갱신 입구**: 적중 시 「결과는 캐시 · N분 전」을 표시하고 「새로 고침」을 제공한다
  (강제 전량 재스캔). 오래된 결과가 방금 스캔한 척하지 않는다.
- **말뭉치 변화 알림**: 캐시 시점의 말뭉치 총수와 현재가 다르면
  「말뭉치가 변했습니다(a → b). 새로 고침을 권장」을 표시. 이 대조는 디렉터리 열거만 하므로 저렴하다
  ——따라서 캐시 무효화 전략 자체가 되지는 않는다.

### 변경

- **단건 되쓰기**: 단일 세션 상태 점검과 가역 처치 후에는 재스캔에 의존하지 않는다——
  처치는 이미 `after` 를 재계산하므로 그것을 그대로 캐시에 되쓴다.
  패널이 목록으로 돌아왔을 때 그 행은 이미 최신 단계다
  (회복된 행은 사라진다. 「문제만 나열」과 일치).
- 캐시는 **디스크에 쓰지 않는다**: DSH 재시작 시 무효. 영속화에는 무효화 전략이 필요하고,
  무효 판정 비용(말뭉치 열거, 로그 mtime/size 대조)은 재스캔 자체에 가깝다. 이번 슬라이스는 YAGNI.

### 테스트

- `tests/cache.spec.ts` 신규 추가(19건): 성립 규칙(미완주 / 완주 / 빈 말뭉치 / 중도 중단 / `clear`),
  단건 되쓰기의 모든 분기(추가 / 교체 / 회복으로 제거 / no-op), 그리고 `handleMethod` 배선
  (`resume` 은 스캔하지 않음, 분할 완주 후 적중, `onlyProblems=false` 는 캐시를 오염시키지 않음,
  처치 후 되쓰기의 객체 동일성, cache 없는 런타임에서도 동작).

## 0.1.0-alpha.2

처치(처방)의 판정과 표시 수정: 「판정 불가」 게이트에 처방하지 않고, 처치 후의 결론을 명시한다.

### 수정

- **`skipped` 에 대한 오처방(중요)**: `prescribe()` 는 기존에 `level !== 'ok'` 로 프로젝션 캐시
  격리 여부를 판단했기 때문에, 「이 게이트는 판정 불가」도 「프로젝션 캐시 미정합」으로 간주되어
  자신의 근거와 모순되는 처방을 내고 **존재하지 않는** 기록의 격리를 제안했다.
  공유 술어 `isActionable()`(`warn` / `fail` 만 처치 가능)을 추출해 일원화했다.
  `gates.ts` 의 단계 기준과 `repair.ts` 의 처치 기준이 이제 같은 의미가 된다.
- **「사문 전록」 오탐**: 처치 항목이 없을 때 고정으로 「사문 전록(四門全綠)」을 표시했다.
  게이트가 `skipped`(판정 불가이며 녹색이 아님)일 때 이 문구는 성립하지 않는다.
  「처치 항목 없음(N 게이트 판정 불가, 이상 아님): <게이트 id 목록>」으로 변경했다.
- **처치 후에도 이상으로 표시되어 처치가 효과가 있었는지 알 수 없음**: 패널은 「처치 전 / 처치 후」의
  단계만 렌더링했기 때문에 둘 다 이상일 때 「처치 실패」와 「처치가 병소와 무관」을 구분할 수 없었다.
  호스트에 `assessRepair({ before, after, repair })` 를 추가해 5단계 중 하나와 사람용 설명을 반환한다:
  `nothing-to-do`(사문 전록) / `repaired`(회복) / `repaired-with-residual`(캐시는 격리했으나
  캐시와 무관한 이상이 잔존, 예: 세션 로그의 open step) /
  `not-applicable`(현재 이상에 가역 처치 항목이 없음) / `failed`(처치 자체의 실패).
  패널은 단계별 배색으로 결론 배너를 렌더링한다.

### 설명

- **처치 능력은 불변**: 「프로젝션 캐시 기록의 격리만, 복사 후 이동, 세션 로그는 개서하지 않음」.
  이번에는 **판정**과 **표시**만 수정했다. 대표적 사례는 「`cold-read` 만 이상
  (세션에 미결제 open step 이 있음)」——이는 본래 이 플러그인이 복구할 대상이 아니며,
  이제는 `not-applicable` 로 명시하고 사유를 설명한다. 조용히 「처치 후에도 이상」으로
  표시하지 않는다.

### 테스트

- `tests/repair.spec.ts` 신규 추가(12건): `isActionable` 의 네 단계, `prescribe` 가 빈 처방을 내지
  않음, `assessRepair` 5단계 판정의 분기를 커버.

## 0.1.0-alpha.1

상태 점검 패널의 판정과 피드백 수정: 「주의」 전량 오탐을 해소하고 스캔 진행률과 행 내 사유를 추가.

### 수정

- **「주의」 전량 오탐(중요)**: `lossless-json` / `projection-cache` / `cold-read` 세 게이트가
  **판정 불가**일 때(콜드 세션에 핫 프로젝션이 없음, 캐시 기록 미생성, 로그에 turn/end 없음)
  `warn` 을 반환하여 **로드되지 않은 모든 세션이 항상 「주의」** 가 되고 실제 신호가 노이즈에
  묻혔다. 중립 단계 `skipped`(패널 표기는 「미점검」)를 추가하여 「관측 불가」를 담당하게 하고
  **총합 판정에 기여하지 않게** 했다.
  고정한 기준: `warn` = 이상 현상을 관측(근거 있음) / `skipped` = 관측·판정 불가(근거 없음).
  실측: 30개 세션이 「30×주의」 → 「26 정상 / 3 주의 / 1 이상」.
- **목록 행에 사유 미표시**: 결과 목록이 총합 단계만 렌더링해 행끼리 구분되지 않았다.
  비정상(non-ok) 「게이트·단계」 요약(예 `연결성·주의`)을 행 안에 표시하여 개별로 열 필요를 없앴다.

### 추가

- **스캔 진행률**: `session-health-scan` 이 `offset` 분할 호출을 지원하고 `total` 을 반환한다.
  패널은 소배치로 연속 호출해 **실제 진행률**(`n/total · 경과 Xs`)을 자체 누적한다.
  분모를 아직 받지 못한 첫 회차는 불확정 애니메이션. 호스트는 무상태를 유지하며
  동기 루프를 비동기로 바꿀 필요가 없다.
- **처치 비지 표시**: 가역 처치 버튼에 스피너를 추가하고 비활성 상태를 유지.

### 변경

- **탭 이름 변경**: `病案室` → `養老院`(영어 `Records Room` → `Retirement Home`).
  「상태 점검 / 처방 / 퇴원」 메타포와 통일.

## 0.1.0-alpha.0

첫 번째 alpha 버전: 세션 스튜어드(히스토리 파일 + 상태 점검).

### 추가

- **세션 히스토리 파일(기록 보관소)**: `dsh-session-search-toggle`에서 공식 아카이브 집합의 읽기 및 정리 구현을 이관했으며,
  동작은 이관 전과 동등합니다(백업 + 동일 디렉터리 임시 쓰기 + 원자적 교체; 명시적 편집 모드에서만 정리 허용).
  차이점: 목록 출처가 **공식 아카이브 집합 진값**으로 변경되었습니다(registry 우선, 스토리지 파일 폴백). 독립 인덱스가 검색 인덱스 패키지와 함께 떠났기 때문입니다;
  제목 등 메타데이터는 최선 노력 방식입니다(`sessionQuery` 제목 스냅샷 → 프로젝션 캐시 title → 비어 있음).
- **상태 점검(점검 → 처방 → 퇴원)**: 독립적으로 테스트 가능한 네 개의 gate
  (`log-integrity` / `projection-cache` / `lossless-json` / `cold-read`),
  통일된 출력 `{ id, level, evidence, attribution, detail }`; `SessionHealthReport`로 집계됩니다.
- **귀속 인덱스**: 프로필의 `node_modules/<pkg>/lib/*.js`를 정적 스캔하여 「프로젝션 key → 패키지 이름」을 구축하고,
  실패한 gate를 구체적인 플러그인과 필드 경로로 되돌려 지목합니다; 찾지 못하면 사실대로 `unknown`으로 표기합니다.
- **되돌릴 수 있는 처방**: 손상된 프로젝션 캐시 레코드 격리(먼저 복사 백업, 그다음 `.quarantine-<ts>`로 이동) + 명령 목록.
  세션 로그 재작성, 히스토리 데이터 변경, 필드 무단 삭제는 **금지**합니다.
- **두 개의 feature gate**: `historyFiles` / `healthCheck`, 꺼지면 해당 하위 도메인 전체가 등록되지 않고(명시적 disabled 오류),
  클라이언트도 해당 탭을 렌더링하지 않습니다.
- **라우트**: `/session-steward/api/*`, 메서드 이름은 일괄 `session-*`이며, 검색 인덱스의 `/switch-search/api`의 `index-*`와 완전히 격리됩니다.

### 알려진 제한

- `lossless-json` gate는 호스트가 `sessionProjections`(핫 상태)를 노출해야 확정 판정을 내릴 수 있습니다;
  얻지 못하면 사실대로 `warn`으로 강등하며, 추측하지 않습니다.
- 상태 스캔은 기본적으로 최근 30개의 비정상 세션만 나열하며(`limit` 상한 200), 전체 라이브러리 순회를 기본 동작으로 삼지 않습니다.
- 클라이언트 패널은 최소 구현(목록 + 상세 + 3단계 버튼)이며, 가상 스크롤과 일괄 작업은 구현하지 않았습니다.
