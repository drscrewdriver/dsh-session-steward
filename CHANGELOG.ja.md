# 変更履歴

## 0.1.0-alpha.1

健康チェックパネルの判定とフィードバックの修正：「注意」の全件誤報を解消し、スキャン進捗と行内の理由を追加。

### 修正

- **「注意」の全件誤報（重要）**：`lossless-json` / `projection-cache` / `cold-read` の 3 ゲートが
  **判定不能**のとき（コールドセッションにホット投影が無い、キャッシュ記録が未生成、
  ログに turn/end が無い）に `warn` を返していたため、**未ロードの全セッションが常に「注意」**
  となり、本当のシグナルがノイズに埋もれていた。
  中性段階 `skipped`（パネル表記は「未チェック」）を追加し、「観測不能」を担わせ、
  **総合判定には寄与させない**。
  固定した基準：`warn` = 異常現象を観測（根拠あり）／`skipped` = 観測不能・判定不能（根拠なし）。
  実測：30 セッションが「30×注意」→「26 正常 / 3 注意 / 1 異常」。
- **一覧行に原因が出ない**：結果一覧は総合段階のみを描画し、行同士が区別できなかった。
  非 ok の「ゲート·段階」要約（例 `可接続性·注意`）を行内に表示し、個別に開く必要をなくした。

### 追加

- **スキャン進捗**：`session-health-scan` が `offset` による分割呼び出しに対応し `total` を返す。
  パネルは小バッチで連続呼び出しし、**実進捗**（`n/total · 経過 Xs`）を自前で累積する。
  分母が未取得の初回は不確定アニメーション。ホストは無状態のままで、同期ループの非同期化は不要。
- **処置のビジー表示**：可逆処置ボタンにスピナーを追加し、無効状態を維持。

### 変更

- **タブ名変更**：`病案室` → `養老院`（英語 `Records Room` → `Retirement Home`）。
  「健康チェック / 処方 / 退院」のメタファーと統一。

## 0.1.0-alpha.0

最初の alpha 版：セッション・スチュワード（履歴ファイル + 健康チェック）。

### 追加

- **セッション履歴ファイル（カルテ室）**：`dsh-session-search-toggle` から公式アーカイブ集合の読み取りとクリーンアップの実装を移入し、
  挙動は移行前と等価（バックアップ + 同一ディレクトリへの一時書き込み + アトミック置換。明示的な編集モードでのみクリーンアップを許可）。
  差分：一覧の取得元を**公式アーカイブ集合の真値**（registry 優先、ストレージファイルにフォールバック）に変更。独立インデックスは検索インデックスパッケージとともに離れたため。
  タイトルなどのメタデータはベストエフォート（`sessionQuery` のタイトルスナップショット → プロジェクションキャッシュの title → 空）。
- **健康チェック（健診 → 処方 → 退院）**：独立にテスト可能な四つのゲート
  （`log-integrity` / `projection-cache` / `lossless-json` / `cold-read`）を備え、
  統一の出力 `{ id, level, evidence, attribution, detail }`、集約結果は `SessionHealthReport`。
- **帰属インデックス**：profile の `node_modules/<pkg>/lib/*.js` を静的に走査して「プロジェクション key → パッケージ名」を構築し、
  失敗したゲートを具体的なプラグインとフィールドパスに紐付ける。走査できない場合は正直に `unknown` と表示する。
- **可逆的な処方**：破損したプロジェクションキャッシュのレコードを隔離（先にコピーでバックアップし、`.quarantine-<ts>` へ移動）+ コマンド一覧。
  **禁止**事項：セッションログの書き換え、履歴データの変更、フィールドの暗黙的な破棄。
- **二つの feature gate**：`historyFiles` / `healthCheck`。オフのときは対応するサブドメイン全体が登録されず（明示的な disabled エラー）、
  クライアントも対応するタブを描画しない。
- **ルート**：`/session-steward/api/*`。メソッド名は一律 `session-*` で、検索インデックスの `/switch-search/api` の `index-*` とは完全に分離。

### 既知の制限

- `lossless-json` ゲートは、ホストが `sessionProjections`（ホット状態）を公開している場合にのみ確定判定を下せる。
  取得できないときは正直に `warn` へ降格し、推測はしない。
- 健康スキャンは既定で直近 30 件の非健康セッションのみを列挙する（`limit` の上限は 200）。全件走査は既定の挙動としない。
- クライアントパネルは最小実装（一覧 + 詳細 + 三ステップのボタン）で、仮想スクロールと一括操作は未実装。
