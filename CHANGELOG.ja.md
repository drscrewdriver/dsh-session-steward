# 変更履歴

## 0.1.0-alpha.6

### 修正 —— アイコンとラベルの間隔を詰め、狭いサイドバーで隣を潰さない

- **間隔 8px → 4px**: 🧭 と「セッションスチュワード」の間を実測に基づいて詰めました(ユーザー指摘
  「icon と文字の間隔を小さく」)。あわせて自前の左右パディングを `0 12px 0 10px` →
  `0 10px 0 8px` にし、隣の「検索」ピルと揃えました。
- **自分が縮む**: `.dss_entryWrap` を `flex:none` → `flex:0 1 auto; min-width:0` に変更し、
  外側の左マージンを `8px → 6px`。以前は一切譲らなかったため不足分がすべて検索ピルに乗り、
  相手のラベルを一文字に潰していました。現在は不足分を自分で引き受けます(ラベルは省略記号で終端)。
- **レール寸法は 28×28 に戻す**: レールの内容ボックスは 36px しかなく(56px レール − 左右 10px)、
  36×36 のコントロール 2 個は入りません(実測 36+28=64px、14px はみ出し)。
  28+28=56px とし、変更前の 32+28=60px より狭くしました。
- **協調の境界は不変**: あくまで自身の CSS のみを変更し、検索インデックスの値を参照せず、
  インストール有無も仮定しません。

## 0.1.0-alpha.5

### 変更 —— サイドバー入口にラベルを付け、「検索インデックス」と同一行で調和

- **入口に名前が付きました**: フッター入口は 28x28 のアイコン丸ボタン（🧭）だけで、何の入口か伝わりませんでした。
  ワイド表示では 42px の行コントロール **🧭 セッションスチュワード** になり、隣の「検索」ピルと
  同じ高さ・同じ角丸（12px）・同じ文字サイズになります。
- **同一行のジオメトリ協調**: 検索入口側が `flex:1;min-width:0` を宣言して行全体を占有しなくなったため、
  2 つの入口は `sidebar.footer.action` の兄弟要素として同じ行に並びます。本入口はワイド時に
  `margin-left:8px` で間隔を空けます（公式 `ui-settings-general` の `.triggerRow{gap:8px}` に対応）。
  相手プラグインの存在は知らず、値も参照しません —— 協調は各自の CSS 内だけで完結します。
- **レール表示への退避**: `wide=false` では `.dss_footerEntryRail`（36x36 の円、ラベルなし）になり、
  Figma のレール仕様（56px レール / 10px パディング / 36x36 コントロール）と公式レールコントロールに一致します。
  ラベルはワイド時のみ表示されます。
- **コピーの帰属修正**: コンポーネント内にハードコードされていた `'会话管家'` のフォールバックを削除し、
  入口のラベルはパネル見出しと同じ出所（辞書 `panel.title`）になりました。

## 0.1.0-alpha.4

設定カードにドロワー形態を復元：従来は裸の行だけで、「プラグイン」設定セクション内の他プラグインと揃っていなかった。

### 修正

- **設定カードにドロワーの外殻が無い（重要）**：DSH の「プラグイン」設定セクション
  （`dsh-client-ui-settings-plugins`）が行うのはただ一つ——settings 名前空間ごとに
  `settings.plugin.item` を配ること：

      renderSlot('settings.plugin.item', {}, { entryKey: ns })

  外殻は**一切提供されない**。カードのタイトル・説明・開閉はすべてプラグイン自身の持ち物である。
  本プラグインは裸の行しか描画していなかったため、二点が崩れていた：
  1. セクション内の他プラグインの「折りたたみドロワー」と形態が揃わない；
  2. `card.title`（セッション管家）と `card.desc`（履歴ファイル · 健康チェック）が
     **定義されているのに一度も描画されない**死んだ文言になっていた——これが「欠けていたドロワー項目」。

  ヘッダー（タイトル + 説明 + chevron、ブロック全体がクリック可能）を復元し、
  本体は既定で折りたたむ（同セクションの他カードと同じ）。`aria-expanded` は開閉状態を反映する。
- **設定サービスが使えないときの挙動**：以前はカード全体が「設定サービスが利用できません」の一行に
  退化し、**タイトルごと消えていた**。現在はドロワーヘッダーは通常どおり描画し、
  理由は本体にのみ示す——このプラグインがセクションから完全に消えることはない。

### 変更

- 開閉の切替と chevron の回転は `prefers-reduced-motion` に従う。

## 0.1.0-alpha.3

健康チェック結果のキャッシュ：パネルを閉じて開き直すとゼロ遅延で内容が出て、処置後は該当行だけが更新される。

### 追加

- **健康チェック結果のキャッシュ（プロセス内）**：一度の完全スキャン（分割で語料を走り切る）の結果を
  ホストプロセスに保持し、パネルを開き直すとキャッシュをそのまま描画する（全量再スキャンしない）。
  キャッシュは**語料を一周し切ったときだけ**成立する——途中でパネルを閉じても、
  半端な結果が完全な結論のふりをすることはない。
- **`resume` は純粋な読み取り**：`session-health-scan` に `resume: true` を追加。**キャッシュを読むだけでスキャンしない**。
  パネルはマウント時にこれで一度だけゼロコストで探り、命中すれば内容を出し、外れれば何もしない
  （マウント即重作業は前版で指摘された点）。同時に現在の語料総数も返す。
- **生成時刻と更新入口**：命中時は「結果はキャッシュ · N 分前」を表示し、「更新」を用意する
  （強制的に全量再スキャン）。古い結果がスキャン直後のふりをすることはない。
- **語料変化の通知**：キャッシュ時の語料総数と現在が食い違うとき
  「語料が変化しました（a → b）。更新を推奨」と表示。この突き合わせはディレクトリ列挙だけで安い
  ——だからキャッシュ失効戦略そのものにはならない。

### 変更

- **単条の書き戻し**：単一セッションの健康チェックと可逆処置の後は再スキャンに頼らない——
  処置は既に `after` を再計算しているので、それをそのままキャッシュへ書き戻す。
  パネルが一覧へ戻ったとき、その行は既に最新の段階になっている
  （回復した行は消える。「問題のみ列挙」と一致）。
- キャッシュは**ディスクに書かない**：DSH 再起動で失効する。永続化には失効戦略が必要で、
  失効判定のコスト（語料の列挙、ログの mtime/size 照合）は再スキャン自体に近い。本スライスは YAGNI。

### テスト

- `tests/cache.spec.ts` を新規追加（19 件）：成立規則（未走破 / 走破 / 空語料 / 途中中断 / `clear`）、
  単条書き戻しの全分岐（追加 / 置換 / 回復で除去 / no-op）、そして `handleMethod` の配線
  （`resume` はスキャンしない、分割走破後の命中、`onlyProblems=false` はキャッシュを汚さない、
  処置後書き戻しのオブジェクト同一性、cache 無しのランタイムでも動作）。

## 0.1.0-alpha.2

処置（処方）の判定と提示の修正：「判定不能」のゲートに処方せず、処置後の結論を明示する。

### 修正

- **`skipped` への誤処方（重要）**：`prescribe()` は従来 `level !== 'ok'` で投影キャッシュの隔離要否を
  判定していたため、「本ゲートは判定不能」も「投影キャッシュ未整合」と見なされ、
  自身の根拠と矛盾する処方を出し、**存在しない**記録の隔離を提案していた。
  共有述語 `isActionable()`（`warn` / `fail` のみ処置可能）を抽出して一本化。
  `gates.ts` の段階基準と `repair.ts` の処置基準がこれで同一の意味になる。
- **「四門全緑」の誤報**：処置項目が無いとき固定で「四門全緑」と表示していた。
  ゲートが `skipped`（判定不能であり緑ではない）のときこの文言は成立しない。
  「処置項目なし（N ゲートは判定不能、異常ではない）：<ゲート id 一覧>」に変更。
- **処置後も異常と表示され、処置が効いたか分からない**：パネルは「処置前 / 処置後」の段階のみを
  描画していたため、両方が異常のとき「処置失敗」と「処置が病巣と無関係」を区別できなかった。
  ホストに `assessRepair({ before, after, repair })` を追加し、5 段階のいずれかと人向け説明を返す：
  `nothing-to-do`（四門全緑）/ `repaired`（回復）/ `repaired-with-residual`（キャッシュは隔離したが
  キャッシュと無関係の異常が残存、例：セッションログの open step）/
  `not-applicable`（現在の異常に可逆処置項目が無い）/ `failed`（処置自体の失敗）。
  パネルは段階ごとの配色で結論バナーを描画する。

### 説明

- **処置能力は不変**：「投影キャッシュ記録の隔離のみ、コピー後に移動、セッションログは改変しない」。
  今回は**判定**と**提示**のみを修正した。典型例は「`cold-read` のみ異常
  （セッションに未決済の open step がある）」——これは本来本プラグインが修復すべき異常ではなく、
  現在は `not-applicable` と明示して理由を説明する。黙って「処置後も異常」とは表示しない。

### テスト

- `tests/repair.spec.ts` を新規追加（12 件）：`isActionable` の 4 段階、`prescribe` が空の処方を
  出さないこと、`assessRepair` の 5 段階判定の分岐を網羅。

## 0.1.0-alpha.1

健康チェックパネルの判定とフィードバックの修正：「注意」の全件誤報を解消し、スキャン進捗と行内の理由を追加。

### 修正

- **「注意」の全件誤報（重要）**：`lossless-json` / `projection-cache` / `cold-read` の 3 ゲートが
  **判定不能**のとき（コールドセッションにホット投影が無い、キャッシュ記録が未生成、
  ログに turn/end が無い）に `warn` を返していたため、**未ロードの全セッションが常に「注意」**
  となり、本当のシグナルがノイズに埋もれていた。
  中性段階 `skipped`（パネル表記は「未チェック」）を追加し、「観測不能」を担わせ、
  **総合判定には寄与させない**。
  固定した基準：`warn` = 異常現象を観測（根拠あり）／`skipped` = 観測不能・判定不能（根拠なし）。
  実測：30 セッションが「30×注意」→「26 正常 / 3 注意 / 1 異常」。
- **一覧行に原因が出ない**：結果一覧は総合段階のみを描画し、行同士が区別できなかった。
  非 ok の「ゲート·段階」要約（例 `可接続性·注意`）を行内に表示し、個別に開く必要をなくした。

### 追加

- **スキャン進捗**：`session-health-scan` が `offset` による分割呼び出しに対応し `total` を返す。
  パネルは小バッチで連続呼び出しし、**実進捗**（`n/total · 経過 Xs`）を自前で累積する。
  分母が未取得の初回は不確定アニメーション。ホストは無状態のままで、同期ループの非同期化は不要。
- **処置のビジー表示**：可逆処置ボタンにスピナーを追加し、無効状態を維持。

### 変更

- **タブ名変更**：`病案室` → `養老院`（英語 `Records Room` → `Retirement Home`）。
  「健康チェック / 処方 / 退院」のメタファーと統一。

## 0.1.0-alpha.0

最初の alpha 版：セッション・スチュワード（履歴ファイル + 健康チェック）。

### 追加

- **セッション履歴ファイル（カルテ室）**：`dsh-session-search-toggle` から公式アーカイブ集合の読み取りとクリーンアップの実装を移入し、
  挙動は移行前と等価（バックアップ + 同一ディレクトリへの一時書き込み + アトミック置換。明示的な編集モードでのみクリーンアップを許可）。
  差分：一覧の取得元を**公式アーカイブ集合の真値**（registry 優先、ストレージファイルにフォールバック）に変更。独立インデックスは検索インデックスパッケージとともに離れたため。
  タイトルなどのメタデータはベストエフォート（`sessionQuery` のタイトルスナップショット → プロジェクションキャッシュの title → 空）。
- **健康チェック（健診 → 処方 → 退院）**：独立にテスト可能な四つのゲート
  （`log-integrity` / `projection-cache` / `lossless-json` / `cold-read`）を備え、
  統一の出力 `{ id, level, evidence, attribution, detail }`、集約結果は `SessionHealthReport`。
- **帰属インデックス**：profile の `node_modules/<pkg>/lib/*.js` を静的に走査して「プロジェクション key → パッケージ名」を構築し、
  失敗したゲートを具体的なプラグインとフィールドパスに紐付ける。走査できない場合は正直に `unknown` と表示する。
- **可逆的な処方**：破損したプロジェクションキャッシュのレコードを隔離（先にコピーでバックアップし、`.quarantine-<ts>` へ移動）+ コマンド一覧。
  **禁止**事項：セッションログの書き換え、履歴データの変更、フィールドの暗黙的な破棄。
- **二つの feature gate**：`historyFiles` / `healthCheck`。オフのときは対応するサブドメイン全体が登録されず（明示的な disabled エラー）、
  クライアントも対応するタブを描画しない。
- **ルート**：`/session-steward/api/*`。メソッド名は一律 `session-*` で、検索インデックスの `/switch-search/api` の `index-*` とは完全に分離。

### 既知の制限

- `lossless-json` ゲートは、ホストが `sessionProjections`（ホット状態）を公開している場合にのみ確定判定を下せる。
  取得できないときは正直に `warn` へ降格し、推測はしない。
- 健康スキャンは既定で直近 30 件の非健康セッションのみを列挙する（`limit` の上限は 200）。全件走査は既定の挙動としない。
- クライアントパネルは最小実装（一覧 + 詳細 + 三ステップのボタン）で、仮想スクロールと一括操作は未実装。
